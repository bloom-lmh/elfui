# @elfui/compiler

Compiler APIs for ElfUI Macro components and build-tool integrations.

Most projects should use `@elfui/vite-plugin` instead of importing this package directly.

```ts
import { compileMacroComponent } from "@elfui/compiler";
```

This package is intended for tooling authors and advanced integrations.
