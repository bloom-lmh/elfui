# @elfui/shared

Shared internal utilities used by ElfUI packages.

This package is published to satisfy the framework dependency graph. Application code should normally import from `@elfui/core`, `@elfui/reactivity`, or another public ElfUI entry instead.
